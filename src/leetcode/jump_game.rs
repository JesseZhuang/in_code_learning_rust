/// 55 medium

/// dfs O(V+E) O(N^2) time and space
pub fn can_jump(nums: Vec<i32>) -> bool {
    let mut visisted = vec![false; nums.len()];
    for (i, num) in nums.iter() {
        if !visisted[i] {}
    }
}
